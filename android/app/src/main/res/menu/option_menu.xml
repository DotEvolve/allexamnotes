<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/option_add_comment"
        android:icon="@drawable/ic_add_comment"
        android:visible="false"
        android:title="Add comment"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/option_search"
        android:icon="@drawable/ic_search"
        android:visible="false"
        android:title="Search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item android:id="@+id/option_save"
        android:icon="@drawable/ic_save"
        android:visible="false"
        android:title="Save Post"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/option_share"
        android:icon="@drawable/ic_share"
        android:visible="false"
        android:title="Share"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/option_publish"
        android:icon="@drawable/ic_send"
        android:visible="false"
        android:title="Publish"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/option_download"
        android:icon="@drawable/ic_file_download"
        android:visible="false"
        android:title="Download"
        app:showAsAction="ifRoom"/>




</menu>